package edu.eci.arsw.alexandria.service;

import org.springframework.http.codec.ServerSentEvent;
import reactor.core.publisher.Flux;
import reactor.core.publisher.Mono;
import java.util.List;

public interface AlexandriaBlackboardService {
    /*Flux<Blackboard> getBlackboards();
    Mono<Blackboard> getBlackboardById(String id);
    Mono<Blackboard> updateBlackboard(Editor editor);
    Mono<Void> deleteBlackboard(String id);
    Mono<Blackboard> addBlackboard(Editor editor);
    Mono<Blackboard> create();*/
}

